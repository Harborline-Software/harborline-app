{
  // Development-only: `dotnet run` still works standalone, but the fixture is now an
  // EXPLICIT opt-in (ticket 153). A non-Development host must configure FormsAdmin:BaseUrl
  // or set this deliberately — a missing origin is a loud startup error, never a silent
  // fixture that mints successful "writes" with no server.
  "FormsAdmin": {
    "UseFixture": true
  }
}
