{
  // The per-boot credential stays server-side (LocalNode__SessionToken).
  "LocalNode": {
    "SessionToken": ""
  },
  // Released catalogue definitions come from the node (Workshop__BaseUrl).
  "Workshop": {
    "BaseUrl": ""
  }
}
